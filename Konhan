<!DOCTYPE html>
<html lang="my">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday ၵွၼ်ႁၢၼ် 🎂</title>
<style>
  *{box-sizing:border-box}
  html,body{margin:0;min-height:100%;font-family:system-ui,-apple-system,"Segoe UI","Noto Sans Myanmar",sans-serif}
  body{
    overflow-x:hidden;color:#fff;
    background:
      radial-gradient(circle at 20% 20%,rgba(255,255,255,.18),transparent 24%),
      radial-gradient(circle at 80% 10%,rgba(255,120,210,.25),transparent 25%),
      linear-gradient(135deg,#24105f,#8a176e 48%,#ff6f91);
  }
  .stars,.hearts{position:fixed;inset:0;pointer-events:none;overflow:hidden}
  .star,.heart{position:absolute;animation:float linear infinite;opacity:.75}
  .star{font-size:12px}.heart{font-size:20px}
  @keyframes float{
    from{transform:translateY(110vh) rotate(0deg);opacity:0}
    15%{opacity:.8} 85%{opacity:.8}
    to{transform:translateY(-15vh) rotate(360deg);opacity:0}
  }
  .wrap{min-height:100vh;display:grid;place-items:center;padding:30px 16px;position:relative;z-index:2}
  .card{
    width:min(920px,100%);padding:34px 22px 30px;text-align:center;
    background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.25);
    border-radius:30px;backdrop-filter:blur(16px);
    box-shadow:0 30px 80px rgba(0,0,0,.28);
    animation:enter 1s ease both;
  }
  @keyframes enter{from{opacity:0;transform:translateY(25px) scale(.97)}to{opacity:1;transform:none}}
  .badge{display:inline-block;padding:8px 15px;border-radius:999px;background:rgba(255,255,255,.16);font-size:14px}
  h1{font-size:clamp(42px,9vw,82px);line-height:1;margin:18px 0 8px;
     text-shadow:0 0 25px rgba(255,255,255,.35)}
  .name{color:#ffe6a7}
  .subtitle{font-size:clamp(18px,3vw,25px);opacity:.94}
  .cake{font-size:110px;line-height:1;margin:18px 0;filter:drop-shadow(0 12px 18px rgba(0,0,0,.25));animation:bounce 2s ease-in-out infinite}
  @keyframes bounce{50%{transform:translateY(-9px) rotate(1deg)}}
  .message{
    max-width:720px;margin:18px auto;padding:24px;
    border-radius:22px;background:rgba(0,0,0,.16);
    font-size:18px;line-height:1.9
  }
  .wish{color:#fff3c4;font-weight:700}
  .btns{display:flex;justify-content:center;gap:12px;flex-wrap:wrap;margin-top:20px}
  button{
    border:0;border-radius:999px;padding:13px 20px;font-size:16px;font-weight:800;
    cursor:pointer;color:#4b124f;background:#fff;
    box-shadow:0 8px 20px rgba(0,0,0,.18);transition:.2s
  }
  button:hover{transform:translateY(-3px) scale(1.02)}
  .secondary{background:rgba(255,255,255,.14);color:#fff;border:1px solid rgba(255,255,255,.28)}
  .footer{margin-top:22px;font-size:13px;opacity:.7}
  #burst{position:fixed;inset:0;pointer-events:none;z-index:5}
  .confetti{position:absolute;width:9px;height:14px;animation:fall 1.7s cubic-bezier(.1,.7,.2,1) forwards}
  @keyframes fall{
    0%{transform:translate(0,0) rotate(0);opacity:1}
    100%{transform:translate(var(--x),var(--y)) rotate(720deg);opacity:0}
  }
  .wishbox{display:none;margin:18px auto 0;max-width:680px;padding:18px;border-radius:18px;background:rgba(255,255,255,.13);line-height:1.8}
  .wishbox.show{display:block;animation:enter .45s ease both}
</style>
</head>
<body>
<div class="stars" id="stars"></div>
<div class="hearts" id="hearts"></div>
<div id="burst"></div>

<main class="wrap">
  <section class="card">
    <div class="badge">🎉 A Special Day • Birthday Celebration 🎉</div>

    <h1>Happy Birthday<br><span class="name">ၵွၼ်ႁၢၼ်</span> 🎂</h1>
    <div class="subtitle">မွေးနေ့မှစပြီး ပျော်ရွှင်မှုတွေနဲ့ အမြဲပြည့်နေပါစေ ✨</div>

    <div class="cake">🎂</div>

    <div class="message">
      <div class="wish">🎈 မွေးနေ့ဆုတောင်း 🎈</div>
      <p>
        ချစ်ခင်ရတဲ့ သူငယ်ချင်း <b>ၵွၼ်ႁၢၼ်</b> ရေ 💖<br>
        ဒီမွေးနေ့ကစပြီး ကောင်းခြင်းတွေ၊ ပျော်ရွှင်ခြင်းတွေနဲ့
        လှပတဲ့အမှတ်တရတွေ အမြဲပိုင်ဆိုင်နိုင်ပါစေ။ 🌟
      </p>
      <p>
        လိုချင်တဲ့အိပ်မက်တွေ တစ်ခုချင်းစီ အကောင်အထည်ဖော်နိုင်ပြီး
        စိတ်ချမ်းသာ၊ ကိုယ်ကျန်းမာကာ နေ့ရက်တိုင်းမှာ
        ပြုံးနိုင်ပါစေ။ 🥳✨
      </p>
      <p><b>Happy Birthday, ၵွၼ်ႁၢၼ်! 🎂🎁🎊</b></p>
    </div>

    <div class="btns">
      <button onclick="celebrate()">🎊 Celebrate!</button>
      <button class="secondary" onclick="showWish()">💌 ဆုတောင်းစာဖတ်မယ်</button>
    </div>

    <div id="wishbox" class="wishbox">
      “မင်းရဲ့ဘဝထဲမှာ အခက်အခဲတွေထက် ပျော်ရွှင်မှုတွေ ပိုများပါစေ။
      မင်းရဲ့အိပ်မက်တွေကို မလျှော့ဘဲ ဆက်လျှောက်နိုင်ပါစေ။
      ဒီနှစ်ဟာ မင်းအတွက် အကောင်းဆုံးအမှတ်တရတွေ ဖန်တီးပေးမယ့်နှစ် ဖြစ်ပါစေ။ 💜”
    </div>

    <div class="footer">Made with friendship & lots of good wishes 💫</div>
  </section>
</main>

<script>
const stars = document.getElementById("stars");
const hearts = document.getElementById("hearts");

for(let i=0;i<55;i++){
  const s=document.createElement("div");
  s.className="star"; s.textContent=Math.random()>.5?"✦":"·";
  s.style.left=Math.random()*100+"%";
  s.style.animationDuration=(5+Math.random()*8)+"s";
  s.style.animationDelay=(-Math.random()*10)+"s";
  stars.appendChild(s);
}
for(let i=0;i<18;i++){
  const h=document.createElement("div");
  h.className="heart"; h.textContent=["💖","💗","💜","✨"][i%4];
  h.style.left=Math.random()*100+"%";
  h.style.animationDuration=(7+Math.random()*8)+"s";
  h.style.animationDelay=(-Math.random()*12)+"s";
  hearts.appendChild(h);
}

function celebrate(){
  const burst=document.getElementById("burst");
  burst.innerHTML="";
  const symbols=["🎉","🎊","✨","💖","⭐","🎈"];
  for(let i=0;i<90;i++){
    const c=document.createElement("div");
    c.className="confetti";
    c.textContent=symbols[Math.floor(Math.random()*symbols.length)];
    c.style.left="50%"; c.style.top="42%";
    c.style.setProperty("--x",(Math.random()*1100-550)+"px");
    c.style.setProperty("--y",(Math.random()*850-300)+"px");
    c.style.animationDelay=(Math.random()*.35)+"s";
    c.style.fontSize=(12+Math.random()*16)+"px";
    burst.appendChild(c);
  }
  setTimeout(()=>burst.innerHTML="",2300);
}
function showWish(){
  document.getElementById("wishbox").classList.toggle("show");
  celebrate();
}
setTimeout(celebrate,700);
</script>
</body>
</html>
